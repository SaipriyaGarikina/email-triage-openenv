[project]
name = "email-triage-openenv"
version = "2.0.0"
description = "Email triage OpenEnv environment"
dependencies = [
    "openenv>=0.2.0",
    "fastapi",
    "uvicorn",
    "pydantic"
]

[project.scripts]
server = "server.app:main"
